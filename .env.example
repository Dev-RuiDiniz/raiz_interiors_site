# Variáveis mínimas para execução local e CI
# Copie para .env.local (desenvolvimento) ou configure no provedor de deploy.

# Banco de dados (obrigatória para rotas que acessam Prisma)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me-with-a-long-random-secret"

# Integração Instagram (Apify) - necessária apenas para /api/instagram/sync
APIFY_API_TOKEN=""

# Opcional: perfil alvo para scraping (se futuramente exposto via código)
# APIFY_INSTAGRAM_USERNAME="raiz.interiors.living"
